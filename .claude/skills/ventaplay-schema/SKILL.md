---
name: ventaplay-schema
description: VentaPlay database schema reference. Use when working on Supabase queries, migrations, RLS policies, Edge Functions, or any task requiring table/column/FK knowledge. Covers all 170 tables across 20 functional domains.
---

# VentaPlay Database Schema Reference

## Description

**Trigger this skill when:** Working on database queries, Supabase client calls, migrations, RLS policies, Edge Functions, or any task that requires knowledge of table structures, column types, foreign key relationships, or constraints.

This skill provides a complete map of the VentaPlay PostgreSQL schema (Supabase). All 170 tables in the `public` schema are organized into 20 functional domains with full column details, FK relationships, and constraints in the `references/` files.

## Quick Reference

- **170 tables** in `public` schema
- **~305 FK relationships**
- **Multi-tenant**: Most tables have `organizacion_id` → `organizaciones.id`
- **IDs**: UUID with `gen_random_uuid()` default
- **Timestamps**: `created_at`/`updated_at` with `now()` default
- **Supabase config**: `max_rows = 1000` — never rely on `.range(0, 9999)` for "all" rows

## Domain Map

| Domain | Tables | Key Tables | Reference |
|--------|--------|------------|-----------|
| CRM & Contacts | 15 | contactos, clientes, cliente_tags, empresas | [crm-contacts.md](references/crm-contacts.md) |
| Appointments & Calendar | 9 | citas, agenda_disponibilidad, organizacion_bloqueos, configuraciones_agenda, google_calendar_connections, google_calendar_event_map, google_calendar_sync_log | [appointments-calendar.md](references/appointments-calendar.md) |
| Services & Professionals | 20 | servicios, profesionales, servicio_profesional, servicio_categorias, servicio_precios_sucursal | [services-professionals.md](references/services-professionals.md) |
| Atenciones (Sessions) | 9 | atenciones, atencion_platos, platos, mermas_pedido | [atenciones-sessions.md](references/atenciones-sessions.md) |
| Payments & Billing | 13 | medios_pago, link_pagos, ingresos, egresos, reembolsos | [payments-billing.md](references/payments-billing.md) |
| Subscriptions | 6 | suscripciones, pagos_suscripcion, plataformas_suscripcion | [subscriptions.md](references/subscriptions.md) |
| Sales & Products | 9 | productos, productos_stock, ventas_externas, consumo_ayudantes | [sales-products.md](references/sales-products.md) |
| Stock & Inventory | 5 | movimientos_stock, proveedores_stock | [stock-inventory.md](references/stock-inventory.md) |
| Point of Sale (POS) | 9 | pos_devices, pos_audit_log, autopago_configs, impresoras_configuradas, impresion_destinos, comanda_print_jobs | [pos.md](references/pos.md) |
| WhatsApp | 6 | configuracion_whatsapp_business, whatsapp_plantillas_meta | [whatsapp.md](references/whatsapp.md) |
| Voice | 3 | voice_calls, voice_call_events | [voice.md](references/voice.md) |
| Email | 5 | configuracion_email, email_templates, email_logs, email_events, email_domains | [email.md](references/email.md) |
| Chat | 5 | conversations, chat_sessions | [chat.md](references/chat.md) |
| Funnels & Opportunities | 7 | funnels, oportunidades, estados_funnel, oportunidad_tags | [funnels-opportunities.md](references/funnels-opportunities.md) |
| Campaigns & Surveys | 10 | campanas, campanas_criterios, campana_envios, respuestas, typeform | [campaigns-surveys.md](references/campaigns-surveys.md) |
| Flows & Automations | 4 | flujos_configuracion, flujos_ejecuciones | [flows-automations.md](references/flows-automations.md) |
| Documents & AI | 6 | documents, document_chunks, token_usage | [documents-ai.md](references/documents-ai.md) |
| Organizations & Config | 11 | organizaciones, sucursales, app_global_config, menus_configuracion, menus_items | [organizations-config.md](references/organizations-config.md) |
| Auth & Roles | 7 | usuarios, user_sessions, roles, user_roles, permissions, autorizaciones_otp | [auth-roles.md](references/auth-roles.md) |
| Tasks & Misc | 9 | tareas, bug_reports, internal_video_recordings, academy_videos, academy_topics | [tasks-misc.md](references/tasks-misc.md) |
| Taller / Cotizaciones | — | vehiculos, ingresos_vehiculares, ingresos_vehiculares_medios, ingresos_vehiculares_eventos, cotizaciones, cotizaciones_items, cotizaciones_eventos, cotizaciones_glosas | → **`ventaplay-taller`** skill (gated by `organizaciones.use_taller`) |

> **Table-count caveat:** the **169** count covers the 20 functional domains above. The **Taller / Cotizaciones** vertical (`vehiculos`, `ingresos_vehiculares`, `ingresos_vehiculares_medios`, `ingresos_vehiculares_eventos`, `cotizaciones`, `cotizaciones_items`, `cotizaciones_eventos`, `cotizaciones_glosas`) is documented in the separate **`ventaplay-taller`** skill and is **not** included in this count — it is gated per-org by `organizaciones.use_taller`. A full `CREATE TABLE` census to reconcile the absolute total across both skills is a tracked follow-up.

## Key Entity Relationship Chains

### CRM Core
```
contactos (1:1)→ clientes (1:N)→ cliente_tag_asociaciones (N:1)→ cliente_tags
                  clientes (N:M)→ cliente_empresa (N:M)→ empresas
                  clientes (1:N)→ cliente_interacciones
                  contactos (1:N)→ contacto_mensajes
```

### Appointments
```
citas → contactos (via contacto_id)
citas → servicios (via servicio_id)
citas → profesionales (via profesional_id)
citas → sucursales (via sucursal_id)
citas → organizaciones (via organizacion_id)
agenda_disponibilidad → profesionales + servicios + organizaciones
```

### Atenciones (Service Sessions)
```
atenciones → contactos + servicios + profesionales + organizaciones
atenciones (1:N)→ atencion_platos (1:N)→ atencion_plato_componentes
atenciones (1:N)→ atenciones_logs
ingreso_atenciones → atenciones + ingresos
```

### Payments
```
ingresos → organizaciones + contactos + medios_pago
link_pagos → organizaciones
ventas_externas (1:N)→ ventas_externas_items → productos_stock
ventas_externas → ventas_externas_boletas_electronicas
atenciones → atenciones_boletas_electronicas
egresos → organizaciones + usuarios + sucursales (no atencion / no medio_pago FK; medio_pago is enum)
egresos (self-FK)→ egresos.id (egreso_original_id, for tipo='reverso_egreso')
```

### Subscriptions
```
suscripciones → clientes_suscripcion + plataformas_suscripcion + organizaciones
pagos_suscripcion → suscripciones
configuracion_suscripcion → organizaciones
```

### Funnels & Opportunities
```
funnels (1:N)→ estados_funnel
oportunidades → funnels + estados_funnel + contactos + organizaciones
oportunidades (1:N)→ historial_oportunidades
oportunidades (1:N)→ actividades_oportunidad
oportunidades (N:M via oportunidad_tag_asociaciones)→ oportunidad_tags
```

### Services & Professionals
```
servicios (N:1)→ servicio_categorias (FK servicios.categoria_id, ON DELETE SET NULL)
servicios (N:M via servicio_profesional)→ profesionales
servicios (N:M via servicio_tags/servicio_tag_asociaciones)→ tags
servicios (N:M via producto_servicio)→ productos
profesionales → usuarios (via usuario_profesionales)
profesionales → categorias_profesionales
```

### Flows & Automations
```
flujos_configuracion (1:N)→ flujos_ejecuciones (1:N)→ flujos_pasos_ejecucion
automatizaciones_ejecuciones (standalone, tracks automation runs)
```

### Multi-Tenant Pattern
```
organizaciones ←── (organizacion_id FK on ~90% of tables)
organizaciones (1:N)→ sucursales
organizaciones (1:1)→ organizaciones_facturacion
usuarios (N:M via user_roles)→ roles (1:N)→ role_permissions → permissions
```

## How to Use

1. **Find which domain a table belongs to**: Check the Domain Map above
2. **Get full column details**: Read the corresponding `references/*.md` file
3. **Understand relationships**: Check the FK chains above or `references/relationships.md`
4. **Build Supabase queries**: Use FK names for `.select()` joins — FK constraint names follow pattern `tablename_columnname_fkey`

### Supabase Query Patterns

```typescript
// Basic nested select following FKs
const { data } = await supabase
  .from('contactos')
  .select('*, clientes(*)')

// INNER JOIN filtering with renamed embed (avoids fetching all rows)
const { data } = await supabase
  .from('contactos')
  .select('*, clientes!clientes_contacto_id_fkey!inner(*, filter_tags:cliente_tag_asociaciones!inner(tag_id))')
  .in('clientes.filter_tags.tag_id', selectedTagIds)

// Never use .in() with large UUID arrays — PostgREST serializes in URL
// Instead use !inner joins or RPC functions
```

## Schema Last Updated
2026-06-10 — Updated through migration 20260707120001 (Impresión V2: 3 NUEVAS TABLAS `impresion_destinos` + `impresion_destino_categorias` + `comanda_print_jobs`; NUEVA TABLA `servicio_precios_sucursal` + resolver RPCs; `finalizar_atencion_con_log` cierra consumos + registra finalizado_por; `medios_pago.campos_config` + `ingresos.datos_medio_pago`; `atencion_platos.comanda_lista`; `consumos.work_resumed_at`). Total tables 165 → 169.

### Recent changes (2026-06-10 — Impresión V2, precio por sucursal, tiempo activo de consumos, medios de pago configurables)

Catorce migraciones posteriores a `20260625120000` (`20260626120000` → `20260707120001`). Cuatro tablas nuevas en este skill (165 → 169): las 3 de **Impresión V2** (POS 6 → 9) y `servicio_precios_sucursal` (Services & Professionals 19 → 20). Tres migraciones pertenecen a la vertical **Taller** y se documentan en el skill `ventaplay-taller` (con un cruce en `link_pagos`). Todas aditivas; la única no-aditiva es el DROP+ADD del CHECK de `comanda_print_jobs.status` y el DROP+CREATE de `get_ingresos_con_datos_v2` (shape del RETURNS TABLE).

- **Impresión V2 — destinos configurables + jobs por destino (3 NUEVAS TABLAS)** (migs `20260629120000_impresion_destinos_v2.sql`, `20260630120000_impresion_destinos_v2_permissions_guards.sql`, `20260707120000_comanda_print_jobs_cleanup.sql`) — reemplaza el modelo legacy de 2 roles fijos (localStorage + flag `comanda_impresa`) por **destinos** por terminal con ruteo por categoría y **cola de jobs**:
  - **`impresion_destinos`** — una impresora por fila, por `terminal_id`, con capacidades `supports_precuenta`/`supports_comanda`, transporte `usb|network` (mismas columnas que `impresoras_configuradas`), `legacy_role` para destinos migrados del modelo V1, `activo`. UNIQUE parcial `(organizacion_id, terminal_id, legacy_role) WHERE legacy_role IS NOT NULL` (mig `20260630120000`, anti doble-clic en "Convertir legacy").
  - **`impresion_destino_categorias`** — N:M destino ↔ `categorias_productos`: qué categorías imprimen comanda en cada destino (bar solo bebidas, cocina solo platos). UNIQUE `(destino_id, categoria_id)`.
  - **`comanda_print_jobs`** — estado de impresión **por destino e ítem** (`item_type` `atencion_platos|consumos` + `item_id` UUID polimórfico SIN FK). Ciclo: `pending → claimed → printed`, con `error`+`retry_count` y `cancelled` (terminal, agregado en `20260707120000`). UNIQUE `(destino_id, item_type, item_id)`. Índice parcial de cola `WHERE status IN ('pending','error')`. Reemplaza `comanda_impresa` como fuente multi-destino (el flag legacy NO se altera).
  - **Garbage collection anti head-of-line blocking** (mig `20260707120000`, post-mortem Casona Asturias): jobs huérfanos (atención cancelada/finalizada post-encolado) tapaban la ventana de 50 `pending` del consumidor → comandas nuevas jamás imprimían. Capa 1: trigger `cancel_comanda_jobs_on_cancelada` ON `atenciones` (AFTER UPDATE OF estado → `cancelada`) cancela jobs abiertos. Capa 3: janitor pg_cron `cleanup-comanda-print-jobs` cada 10 min — jobs abiertos de atenciones `cancelada|finalizada` con >15 min de gracia + TTL duro 6 h. Capa 2 (no re-encolar muertos) vive en app (`printEngineV2`).
  - **RLS completo org-scoped** en las 3 tablas (mismo patrón que `impresoras_configuradas`) + GRANTs CRUD a `anon, authenticated, service_role` (mig `20260630120000` — sin ellos, 42501). See [pos.md](references/pos.md).

- **`servicio_precios_sucursal` (NUEVA TABLA) + `servicios.usar_precio_por_sucursal` + resolver RPCs** (mig `20260707120001_add_precio_por_sucursal_servicios.sql`) — precio por sucursal opt-in para servicios `tipo='individual'` (V1; packs lo ignoran, UI fuerza false). Flag false (default) → `servicios.precio` global byte-equivalente; true → precio de `servicio_precios_sucursal` con **fallback a `servicios.precio`** si no hay fila o sucursal NULL. UNIQUE `(servicio_id, sucursal_id)`, CHECK `precio >= 0`, **SIN RLS** (postura grants-only, espejo `stock_por_ubicacion`). RPCs **única fuente de verdad** del precio efectivo: `resolver_precio_servicio(servicio, sucursal) → numeric` (escalar) y `resolver_precios_servicios(jsonb) → TABLE` (batch, delega en el escalar); ambas STABLE SECURITY DEFINER granted a anon/authenticated/service_role. Las consumen frontend (display + cobro), edge functions y MCP tools para que precio mostrado == precio cobrado. See [services-professionals.md](references/services-professionals.md).

- **Tiempo activo de consumos: `consumos.work_resumed_at` + cierre centralizado en `finalizar_atencion_con_log`** (migs `20260626120000`, `20260627120000`, `20260628120000`, `20260629100000`) — `work_resumed_at timestamptz NULL` ancla el segmento activo tras REABRIR un consumo finalizado; `duration` ahora es **acumulativa** (`+= minutos desde COALESCE(work_resumed_at, created_at)`, no replace). El RPC `finalizar_atencion_con_log` fue reescrito 3 veces (misma firma → CREATE OR REPLACE): (1) paso 3b cierra TODOS los consumos de servicio pendientes al finalizar la atención (omite `finished_at NOT NULL` y productos) — todos los puntos de finalización lo heredan (AtencionDetalle, calendario, edge functions TUU/MP); (2) fix SQLSTATE 42702 calificando `consumos.*` (colisión con la variable OUT `atencion_id` del RETURNS TABLE); (3) persiste `atenciones.finalizado_por_id/_nombre` en la misma transacción (antes solo log + UPDATE fire-and-forget del cliente), con sentinel zero-UUID → NULL (sistema). See [sales-products.md](references/sales-products.md#consumos) + [atenciones-sessions.md](references/atenciones-sessions.md).

- **`atencion_platos.comanda_lista`** (mig `20260704120000_add_comanda_lista_to_atencion_platos.sql`) — `boolean NOT NULL DEFAULT true`. Fase 1 del fix de **comandas partidas**: la confirmación de pedido inserta platos uno a uno (lento — receta + stock por insert) y el ciclo de auto-impresión de 5s podía imprimir una comanda parcial a mitad de inserción. Ahora la confirmación inserta `comanda_lista = false` (invisible al motor) y libera TODO con un UPDATE atómico al final ("todo o nada"). DEFAULT true → filas existentes y flujos que no usan confirmación en dos fases siguen igual. See [atenciones-sessions.md](references/atenciones-sessions.md#atencion_platos).

- **Medios de pago con campos configurables** (migs `20260705120000_medios_pago_campos_configurables.sql` + `20260706120000_get_ingresos_con_datos_v2_datos_medio_pago.sql`) — `medios_pago.campos_config jsonb NOT NULL DEFAULT '[]'` define campos personalizados pedidos al cobrar (shape `[{key, label, tipo: 'texto'|'numero'|'select', obligatorio, opciones?}]`); `ingresos.datos_medio_pago jsonb NULL` guarda los valores capturados (`{key: valor}`). `[]`/NULL = comportamiento legacy byte-equivalente; no toca `requiere_pos`/`requiere_pos_manual`. `get_ingresos_con_datos_v2` gana la columna `datos_medio_pago` al final del RETURNS TABLE — **DROP + CREATE** (cambia el shape, no se puede CREATE OR REPLACE) + re-GRANT. See [payments-billing.md](references/payments-billing.md).

- **Vertical Taller (3 migs, documentadas en el skill `ventaplay-taller`, NO suman al conteo)** — `20260701120000_cotizaciones_glosas.sql` (NUEVA TABLA `cotizaciones_glosas`: biblioteca de cláusulas reutilizables por org, `por_defecto` pre-llena `cotizaciones.terminos_condiciones`); `20260702120000_cotizaciones_prepago.sql` (`cotizaciones.prepago_requerido`/`prepago_porcentaje`, **`link_pagos.cotizacion_id`** FK SET NULL + trigger `tr_link_pagos_cotizacion_on_paid` que auto-acepta la cotización al pagarse el anticipo — el lado `link_pagos` SÍ está documentado en [payments-billing.md](references/payments-billing.md) por ser tabla de este skill; reescribe `upsert_cotizacion_con_items`); `20260703120000_ingresos_vehiculares_compartir.sql` (`ingresos_vehiculares.public_token`/`fecha_envio`/`fecha_vista_cliente` + NUEVA TABLA `ingresos_vehiculares_eventos`, espejo de `cotizaciones_eventos`).

### Recent changes (2026-06-03, II — `impresoras_configuradas` printing inventory + `consumos.finished_at` / `duration`)

Tres migraciones (`20260623120000`–`20260625120000`) posteriores al sync previo. Una crea una **tabla nueva** (`impresoras_configuradas`, +1 → total 165) y dos agregan columnas a `consumos`. Todas aditivas — sin operaciones destructivas, sin NOT NULL sobre columnas existentes, sin backfill.

- **`impresoras_configuradas` (NUEVA TABLA)** (migration `20260623120000_impresoras_configuradas.sql`) — inventario **administrativo** de impresoras térmicas por **terminal (`terminal_id`) + rol** (`mesa-precuenta` | `comanda-cocina`). **NO reemplaza** el `localStorage` operativo (`receiptPrinter:<org>:<role>`): la impresión real sigue leyendo localStorage; esta tabla es solo registro/visibilidad administrativa en V1 (no es la fuente de verdad del motor de impresión). Columnas: `id`, `organizacion_id` FK→organizaciones CASCADE, `terminal_id text NOT NULL`, `terminal_nombre text`, `rol text NOT NULL CHECK IN ('mesa-precuenta','comanda-cocina')`, `nombre text`, `transport_type text NOT NULL CHECK IN ('usb','network')`, ids USB (`vendor_id int`, `product_id int`, `serial_number text`, `device_index int`, `manufacturer_name text`, `product_name text`), red (`host text`, `port int DEFAULT 9100`), `bridge_version text`, `browser_user_agent text`, `last_seen_at timestamptz`, `metadata jsonb NOT NULL DEFAULT '{}'`, timestamps. **UNIQUE `(organizacion_id, terminal_id, rol)`**. Índices: `(organizacion_id, rol)`, `(organizacion_id, terminal_id)`. Trigger `update_updated_at_column()`. **RLS completo org-scoped** (SELECT/INSERT/UPDATE/DELETE gateados por `is_super_admin() OR get_current_custom_user_organization() = organizacion_id`) — **NO** el dev-wide-open de otras tablas operativas. Suma al dominio **Point of Sale (POS)** (5 → 6 tablas); total 164 → 165. See [pos.md](references/pos.md). Contexto de impresión: `.agents/skills/ventaplay-printing/SKILL.md` + sección Thermal Printing en `CLAUDE.md`.

- **`consumos.finished_at`** (migration `20260624120000_add_finished_at_to_consumos.sql`) — `timestamptz NULL`, sin default. Momento en que el consumo (línea de servicio/producto) se marca como finalizado. NULL = aún no finalizado. Mismo tipo que `created_at` pero nullable y seteada al finalizar, no al crear. See [sales-products.md](references/sales-products.md#consumos).

- **`consumos.duration`** (migration `20260625120000_add_duration_to_consumos.sql`) — `integer NULL`, sin default, con `CONSTRAINT consumos_duration_check CHECK (duration >= 0)`. Duración del consumo en **minutos**. NULL = aún no establecida. Companion de `finished_at`; sin backfill (filas existentes quedan NULL). Nota Postgres: `CHECK (duration >= 0)` permite NULL (NULL >= 0 evalúa a NULL, no FALSE) → no necesita `IS NULL OR`. See [sales-products.md](references/sales-products.md#consumos).

### Recent changes (2026-06-03 — 5º criterio v3 `servicio_realizado` + menú dinámico por organización)

Dos migraciones posteriores a `20260620120000`, ambas **100% aditivas** — sin tablas nuevas, sin operaciones destructivas, sin NOT NULL sobre columnas existentes, sin cambios de firma que rompan callers. Total de tablas sin cambios en **164**.

- **5º criterio de targeting v3 de Campañas: `servicio_realizado`** (migration `20260622120000_campanas_criterio_servicio.sql`) — agrega un criterio que segmenta por **"se hizo tal servicio"**, con filtros opcionales de fecha y sucursal embebidos en `valor_jsonb`. **Semántica exigente**: el cliente entra solo si tiene ≥1 atención efectiva DONDE, **en esa misma atención**, se realizó el servicio — NO el producto cartesiano engañoso "tiene alguna atención en el rango" Y "alguna vez se hizo el servicio". El `INNER JOIN consumos ON consumos.atencion_id = a.id AND consumos.servicio_id = p_servicio_id` es el corazón que lo garantiza.
  - **CHECK `campanas_criterios_categoria_check` extendido** (DROP + ADD): ahora `categoria IN ('tag','funnel','fecha_atencion','sucursal','servicio_realizado')`.
  - **CHECK `campanas_criterios_valor_check` extendido**: `servicio_realizado` se une a `fecha_atencion` en la rama jsonb — `(categoria IN ('tag','funnel','sucursal') AND valor_id NOT NULL AND valor_jsonb NULL) OR (categoria IN ('fecha_atencion','servicio_realizado') AND valor_id NULL AND valor_jsonb NOT NULL)`.
  - **`valor_jsonb` shape para `servicio_realizado`**: `{ servicio_id: uuid, desde?: 'YYYY-MM-DD', hasta?: 'YYYY-MM-DD', sucursal_id?: uuid }`. Cubre 3 casos con 1 categoría: sin fecha = "se hizo X alguna vez"; con fecha = "se hizo X entre Y y Z" (caso principal); con fecha + sucursal = "se hizo X en sucursal Q entre Y y Z".
  - **RPC nueva `get_clientes_por_servicio(p_org_id uuid, p_servicio_id uuid, p_desde date DEFAULT NULL, p_hasta date DEFAULT NULL, p_tz text DEFAULT 'America/Santiago', p_sucursal_id uuid DEFAULT NULL) → table(cliente_id uuid)`** — `SECURITY DEFINER`, `STABLE`, `SET search_path = public`. `p_org_id` + `p_servicio_id` obligatorios (RAISE 22023). Fechas: ambas o ninguna (un solo lado es ambiguo → RAISE 22023) + valida `desde <= hasta`. Solo cuenta atenciones efectivas (`estado IN ('finalizada','en_curso','reabierto')`). Sucursal histórica via `COALESCE(citas.sucursal_id, profesionales.sucursal_id)`. `(fecha_inicio AT TIME ZONE p_tz)::date` para respetar TZ Chile. `DISTINCT cliente_id`. Granted a `authenticated, service_role, anon`. `NOTIFY pgrst, 'reload schema'` al final. Mismo patrón que `get_clientes_por_atencion` / `get_clientes_por_sucursal`. Canonical callers: `useCampanaTargetPreview.resolverCriterio` (rama `servicio_realizado`), `prepare-campana-send`, `send-campana-whatsapp/targeting-service.ts`.
  - **Retrocompat**: campañas existentes no usan este criterio (es nuevo) → cero impacto; los CHECK siguen aceptando las 4 categorías previas. No bumps el conteo de tablas (`campanas_criterios` ya existía). See [campaigns-surveys.md](references/campaigns-surveys.md).

- **`menus_configuracion.organizacion_id`** (migration `20260621120000_add_organizacion_id_to_menus_configuracion.sql`) — `uuid NULL REFERENCES organizaciones(id) ON DELETE SET NULL`. Permite asignar un menú dinámico a una **organización específica**, además de (o en lugar de) la asignación por módulo existente. **Prioridad en runtime: org-específico > módulo**. Dos índices nuevos: parcial `idx_menus_configuracion_organizacion_id` ON `(organizacion_id) WHERE organizacion_id IS NOT NULL`; UNIQUE parcial `idx_menus_configuracion_org_activo` ON `(organizacion_id) WHERE activo = true AND organizacion_id IS NOT NULL` (safety-net: un solo menú activo por org). **Trigger `menus_configuracion_deactivate_siblings()` reescrito** (`CREATE OR REPLACE`) para cubrir también el caso org-específico: al activar un menú con `organizacion_id` set, desactiva los demás menús activos de esa misma org (en adición a la lógica original de desactivar hermanos que comparten módulos). El trigger `tr_menus_configuracion_one_active_per_modulo` se recrea para vigilar también la columna `organizacion_id` (`BEFORE INSERT OR UPDATE OF activo, modulos, organizacion_id`). Sin backfill — la columna nace NULL en toda fila existente (= comportamiento previo, asignación solo por módulo). No bumps el conteo de tablas. See [organizations-config.md](references/organizations-config.md).

### Recent changes (2026-05-29 — menús dinámicos + `cita_agendada_widget` + tag público; cotizaciones-vehículo movido al skill Taller)

Cuatro migraciones posteriores a `20260616120000`. Dos crean la base de **menús dinámicos** (config global de navegación por módulo), una agrega el 14º `tipo_evento` de automatizaciones, y una expone un RPC público para adjuntar tags de cliente desde el widget. Las dos migraciones de cotizaciones del mismo rango (`20260613121000`, `20260613123000`) pertenecen a la vertical **Taller** y se documentan en el skill `ventaplay-taller` (ver caveat en el Domain Map) — no se duplican aquí.

- **`menus_configuracion` + `menus_items` (2 NUEVAS TABLAS)** (migration `20260619120000_create_menus_configuracion.sql`) — config **global** (sin `organizacion_id`) de menús de navegación dinámicos, asignables a módulos vía `modulos text[]` (CHECK ⊆ `{restaurantes, talleres}`). `menus_configuracion`: `id`, `nombre`, `descripcion`, `modulos`, `activo`, timestamps; índice GIN sobre `modulos` + parcial sobre `activo`; trigger `touch_updated_at` + trigger `deactivate_siblings` (un solo menú activo por módulo). `menus_items`: `id`, `menu_id` FK→`menus_configuracion` CASCADE, self-FK `parent_id` CASCADE, `label`, `icon_name`, `route`, `tab_key`, `orden`, `activo`; índices sobre `menu_id` y `(menu_id, parent_id, orden)`. **RLS**: SELECT global (`USING true`), escritura gateada por el nuevo helper `is_ventaplay_internal_user()` (`is_super_admin()` OR org = UUID interno Ventaplay `36924e79-0db9-4fb4-80e8-0cf2c43d4310`). Bumps Organizations & Config domain to 11 tables; total 162 → 164. See [organizations-config.md](references/organizations-config.md).

- **Grants `anon` CRUD sobre tablas de menús** (migration `20260620120000_grant_menus_anon_write.sql`) — `GRANT SELECT, INSERT, UPDATE, DELETE … TO anon, authenticated` en ambas tablas. La app habla con Postgres como rol `anon` (auth custom vía X-Session-Token), así que sin grants explícitos daba `42501 permission denied`. La seguridad real sigue en el RLS (`is_ventaplay_internal_user()` para escritura). Mismo patrón que el resto de tablas operativas.

- **14º `tipo_evento`: `cita_agendada_widget`** (migration `20260618120000_cita_agendada_widget_tipo_evento.sql`) — DROP + ADD del CHECK `automatizaciones_whatsapp_tipo_evento_check` agregando `'cita_agendada_widget'`. Disparado por `widgetAgendamientoService.submitWidgetBooking` cuando un visitante anónimo se auto-agenda desde el widget público (`EmbedAgendamientoPage`) — separado de `cita_agendada` (CRM/MCP) para permitir copy distinto. Mismas variables de template que `cita_agendada`. **Lista completa actual (14 valores)**: `'cita_agendada','cita_cancelada','cita_recordatorio','cliente_nuevo','oportunidad_ganada','cita_agendada_con_abono','atencion_terminada','contacto_sin_respuesta','lead_calificado','cita_reagendada','ejecutivo_asignado','oportunidad_cambio_estado','formulario_enviado','cita_agendada_widget'`. Bumps the `automatizaciones_whatsapp` reference in [whatsapp.md](references/whatsapp.md).

- **RPC `public_attach_cliente_tag(p_cliente_id uuid, p_tag_id uuid) → void`** (migration `20260617120000_public_attach_cliente_tag.sql`) — SECURITY DEFINER, `search_path = public`. Permite al widget público de formularios (rol `anon` vía `supabasePublic`) adjuntar un `cliente_tag` a un cliente recién creado, bypaseando el RLS de `cliente_tag_asociaciones`. Valida cross-org (`clientes.organizacion_id` == `cliente_tags.organizacion_id`, RAISE si difieren) e idempotente (`INSERT … ON CONFLICT (cliente_id, tag_id) DO NOTHING`). Granted to `anon, authenticated, service_role`. Mirror exacto de `public_attach_oportunidad_tag` (`20260412224552`). See [crm-contacts.md](references/crm-contacts.md).

- **Cotizaciones-vehículo (Taller, NO duplicado aquí)** — `20260613123000_add_vehiculo_to_cotizaciones.sql` (agrega `cotizaciones.vehiculo_id uuid NULL REFERENCES vehiculos(id) ON DELETE SET NULL` + índice parcial + reescribe `upsert_cotizacion_con_items` / `cotizaciones_fetch_list` / `cotizaciones_fetch_by_id` para embeber el vehículo) y `20260613121000_fix_cotizaciones_es_principal_rpc.sql` (preserva `cotizaciones_items.es_principal` en el RPC) están **documentadas en el skill `ventaplay-taller`**, dueño de la vertical Taller/Cotizaciones (`vehiculos`, `ingresos_vehiculares`, `cotizaciones*`).

### Recent changes (2026-05-27 — `oportunidad_cambio_estado` + `formulario_enviado` automatizaciones)

Dos nuevos `tipo_evento` para el motor de automatizaciones (`automatizaciones_whatsapp`), cubriendo (1) cambios de estado de oportunidad con filtro configurable por estado destino, y (2) envíos del widget público de formulario. Ambas migraciones son **100% aditivas** — extienden el CHECK constraint y, la primera, agrega una columna nullable + un índice parcial + extiende el shape del RPC `get_automatizaciones_activas`. Cero impacto en automatizaciones existentes (los 11 eventos previos siguen funcionando idéntico).

- **`automatizaciones_whatsapp.estado_destino_id`** (migration `20260615120000_oportunidad_cambio_estado_automatizacion.sql`) — `uuid NULL REFERENCES estados_funnel(id) ON DELETE CASCADE`. Solo aplica para el nuevo `tipo_evento='oportunidad_cambio_estado'`. `trigger-automatizaciones` filtra in-memory las automatizaciones para este evento comparando `auto.estado_destino_id === estado_nuevo_id` antes de dispatchar — automatizaciones con `estado_destino_id IS NULL` se skipean en este path. Para los demás eventos la columna queda NULL y se ignora. ON DELETE CASCADE garantiza que al borrar un estado_funnel, las automatizaciones huérfanas se limpian. Bumps the `automatizaciones_whatsapp` reference in [whatsapp.md](references/whatsapp.md).

- **`idx_auto_estado_destino`** (misma migration) — índice parcial `ON automatizaciones_whatsapp(estado_destino_id) WHERE estado_destino_id IS NOT NULL`. Mantiene el índice chico (la mayoría de automatizaciones no usan este evento).

- **CHECK constraint `automatizaciones_whatsapp_tipo_evento_check` extendido (2 veces)** (migrations `20260615120000` y `20260616120000`) — cada migración hace DROP + ADD del CHECK incluyendo todos los valores previos + el nuevo. Lista final post-2026-05-27: `'cita_agendada','cita_cancelada','cita_recordatorio','cliente_nuevo','oportunidad_ganada','cita_agendada_con_abono','atencion_terminada','contacto_sin_respuesta','lead_calificado','cita_reagendada','ejecutivo_asignado','oportunidad_cambio_estado','formulario_enviado'`.

- **RPC `get_automatizaciones_activas(uuid, text)` extendido a 20 columnas** (mig `20260615120000`) — agrega `estado_destino_id uuid` al `RETURNS TABLE`. Como Postgres NO permite `CREATE OR REPLACE FUNCTION` cuando cambia el shape del `RETURNS TABLE` (SQLSTATE 42P13), la migración hace `DROP FUNCTION + CREATE FUNCTION` + re-`GRANT EXECUTE` para mantener accesibilidad de `anon, authenticated, service_role`. Body byte-equivalente al previo (`20260415120000_add_email_channel_to_automatizaciones.sql`) EXCEPTO por el nuevo SELECT de `a.estado_destino_id`. Caller único en `trigger-automatizaciones/index.ts` lo invoca con `as any` — extra columna ignorada transparentemente. Schema lock durante el DDL previene callers concurrentes durante el sub-milisegundo del DROP+CREATE.

- **Nuevos eventos `tipo_evento`**:
  - **`oportunidad_cambio_estado`**: Disparado por 5 caminos del frontend + 2 del backend cuando una oportunidad cambia de estado_id. Frontend: `useMoveOportunidad` (Kanban drag&drop + sheet detalle), `OportunidadesService.actualizarOportunidad` (cuando updates incluye estado_id), `useContactoAssignment` (admin asigna contacto desde chat → crea oportunidad). Backend: nodo `crear_oportunidad` en `flujos-operations/node-processors.ts` (ambas ramas UPDATE existente + INSERT nueva), cascada desde la acción `mover_funnel` en `trigger-automatizaciones/index.ts`. Loop prevention con counter `_depth` que aborta si > 3. Variables expuestas al template: `oportunidad.estado_anterior` (nombre, vacío si es creación), `oportunidad.estado_nuevo` (nombre del estado destino), `oportunidad.probabilidad`.
  - **`formulario_enviado`** (label visible en UI: *"Formulario Widget"*): Disparado por `ContactFormCRMService.processWidgetFormSubmission` después de crear contacto + cliente + oportunidad. **Importante**: `processContactForm` (usado por landings públicas de cada org) NO dispara este evento — solo `processWidgetFormSubmission` (usado por `EmbedFormularioPage` del widget de captura embebido en sitios externos vía `vp-formulario.js`, **y por el widget de agendamiento** vía `widgetAgendamientoService.ts`). Variables expuestas: `formulario.mensaje` (texto que dejó el visitante), `formulario.titulo` (título configurado en el widget), `formulario.funnel`, `formulario.estado`, `formulario.etiqueta` (resueltos a nombres desde IDs en runtime por `trigger-automatizaciones`).

- **Helpers compartidos para invocación centralizada** (no son schema pero son críticos para entender el flujo de invocación):
  - `src/utils/automatizaciones/triggerOportunidadCambioEstado.ts` (frontend)
  - `supabase/functions/_shared/triggerOportunidadCambioEstado.ts` (Deno)
  - Ambos son fire-and-forget con timeout 5s, skip silencioso si `estado_anterior === estado_nuevo`. Errores se logean como warn pero no se propagan — la mutación principal (UPDATE oportunidad, INSERT oportunidad, etc.) nunca se rompe por fallas en la automatización.

- **Bug fix incluido** (en `trigger-automatizaciones/index.ts`): el bloque de resolución de oportunidad usaba `oportunidades.usuario_asignado_id` que no existe en el schema. Corregido a `responsable_id` antes de llegar a producción.

- **Doc completo**: `docs/IMPLEMENTACION_AUTOMATIZACIONES_FUNNEL_Y_WIDGET.md` (PR de esta sesión).

### Recent changes (2026-05-26 — Criterio sucursal en Campañas v3, PR #897)

Extiende el modelo de targeting v3 de Campañas con **dos formas complementarias** de filtrar por sucursal: (1) criterio independiente `categoria='sucursal'` que combina via Y/O con tag/funnel/fecha; (2) sucursal opcional embebida en el `valor_jsonb` del criterio `fecha_atencion` para consultas tipo "última visita a sucursal X en rango Y". Ambas usan resolución histórica `COALESCE(citas.sucursal_id, profesionales.sucursal_id)` para honrar la sucursal real de la visita (mismo patrón que `get_ingresos_con_datos_v2` y reportes de comisiones).

- **`campanas_criterios.categoria` CHECK extendido** (migration `20260614120000_campanas_criterio_sucursal.sql`) — agrega `'sucursal'` a la lista: `CHECK (categoria IN ('tag','funnel','fecha_atencion','sucursal'))`. El CHECK `valor_check` también se actualiza: `sucursal` se une a `tag`/`funnel` en la rama referencial (`valor_id NOT NULL AND valor_jsonb NULL`). Compatible 100% con campañas existentes — el constraint sigue aceptando las 3 categorías originales sin cambios. `valor_id` para `categoria='sucursal'` apunta a `sucursales.id` (sin FK real — patrón polimórfico igual que tag/funnel). El frontend (`CampanaCriteriosBuilder.CategoriaCommand`) oculta la opción "Sucursal" entera cuando `useSucursalesList().hasSucursales === false`, así que orgs sin sucursales no ven la opción y se mantiene el builder idéntico al previo.

- **`campanas_criterios.valor_jsonb` shape extendido para `fecha_atencion`** (misma migration) — el objeto ahora soporta `sucursal_id?: uuid | null` opcional: `{desde, hasta, modo, sucursal_id?}`. Cuando `sucursal_id` está set, el filtro de fecha se restringe a atenciones en esa sucursal (resolución histórica). Cuando es `null`/`undefined` (campañas pre-feature), considera todas las sucursales — retrocompat total. La defensa `criteriosBdToInput.normalizarFechaJsonb` setea `sucursal_id: v.sucursal_id ?? null` al leer de BD, normalizando campañas pre-feature.

- **RPC `get_clientes_por_sucursal(p_org_id uuid, p_sucursal_id uuid) → table(cliente_id uuid)`** (misma migration) — nueva RPC dedicada al criterio independiente `categoria='sucursal'`. Trae clientes con ≥1 atención efectiva (`estado IN ('finalizada','en_curso','reabierto')`) en la sucursal indicada, vía `COALESCE(citas.sucursal_id, profesionales.sucursal_id)`. SECURITY DEFINER, STABLE, search_path public. Validation: ambos parámetros NOT NULL con RAISE EXCEPTION explícito. Granted to authenticated, anon, service_role. Canonical callers: `useCampanaTargetPreview.resolverCriterio` (frontend), `prepare-campana-send/index.ts` (Phase 1), `send-campana-whatsapp/targeting-service.ts` (legacy WhatsApp programadas).

- **RPC `get_clientes_por_atencion` extendida con `p_sucursal_id uuid DEFAULT NULL`** (misma migration) — sexto parámetro opcional al FINAL de la firma. Cuando `NULL` (default), comportamiento idéntico al previo (sin filtro de sucursal) — **garantiza retrocompat 100% con campañas existentes**. Cuando set, filtra por sucursal resuelta vía COALESCE: en modo `'cualquiera'` agrega cláusula `AND COALESCE(c.sucursal_id, pr.sucursal_id) = p_sucursal_id`; en modo `'ultima'` usa un CTE `atenciones_en_sucursal` para computar la última visita **por (cliente, sucursal específica)**, no la global (un cliente cuya última visita global fue en otra sucursal pero su última visita a X cae en rango sí entra). DROP + CREATE en transacción atómica con `NOTIFY pgrst, 'reload schema'` al final.

- **Validación de huérfanos extendida en `CampanaFormModal`** — la detección de criterios huérfanos (chip rojo + AlertTriangle en el builder + submit bloqueado) ahora cubre: (1) criterio `categoria='sucursal'` con `valor_id` apuntando a sucursal eliminada; (2) criterio `categoria='fecha_atencion'` con `valor_jsonb.sucursal_id` apuntando a sucursal eliminada. Sin esto, el chip rojo aparecía visualmente pero el submit pasaba.

- **`useCampanaTargets` extendido para sucursal** — el query polimórfico agrega embed `sucursales:sucursales!valor_id (id, nombre)` y el filtro `IN ('tag', 'funnel', 'sucursal')`. El tipo `CampanaTarget` gana `sucursal?: { id, nombre }` opcional. Expone `uniqueSucursales` + `sucursalIds` además de los existentes para tag/funnel. `CampanaDetailTabs` y `CampanaTargetsDisplay` muestran ahora las sucursales en su propia sección + indicador "Solo en sucursal X" dentro del card del criterio de fecha cuando aplica.

- **Edge function `send-campana-whatsapp/targeting-service.ts` actualizada** (no es del schema pero es crítica) — el legacy path de campañas WhatsApp programadas (invocado por `execute-scheduled-campanas`) tenía su PROPIA copia del resolver de criterios v3 que requería el mismo upgrade que `prepare-campana-send`. Sin esto, campañas WhatsApp programadas con criterio sucursal devolvían audiencia 0; las que tenían sucursal embebida en fecha ignoraban el filtro silenciosamente.

**Retrocompat garantizada en 3 niveles**: (1) `p_sucursal_id` con `DEFAULT NULL` en la RPC → cláusula `IS NULL OR ...` no filtra; (2) callers usan `v.sucursal_id ?? null` defensivo; (3) frontend normaliza `sucursal_id: v.sucursal_id ?? null` al leer BD. Toda campaña sin `sucursal_id` (todas las pre-feature) se comporta exactamente igual que antes — verificado con 29 tests (18 originales sin regresión + 11 nuevos cubriendo sucursal). See [campaigns-surveys.md](references/campaigns-surveys.md).

### Recent changes (2026-05-25 — Google Meet integration rollout, PR #878)

Phase 1 extension of the Google Calendar integration: per-cita Meet link storage + two per-org feature toggles (email obligatorio + auto-create Meet). All three additions are purely additive — no destructive operations, no data loss, no NOT NULL on existing columns. Migrations are idempotent via `ADD COLUMN IF NOT EXISTS`.

- **`citas.google_meet_url`** (migration `20260611120000_google_meet_y_email_cliente.sql`) — `text NULL`. URL of the Google Meet room for the synchronized event. Populated by the `google-calendar-sync` Edge Function (`handleCreate` + `handleUpdate`) after `fetchMeetUrlWithRetry` extracts the link from `conferenceData.entryPoints` (up to 3 retries × 700ms because Google creates Meet rooms asynchronously — the initial POST response often lacks `entryPoints`). Only populated when (a) the professional has Google Calendar connected, (b) `organizaciones.crear_google_meet_citas = true`, and (c) the cita was synced successfully. The UPDATE is best-effort (try/catch with warn log) — failure to write the column does NOT break the cita create flow. **Read paths**: `useCitas.obtenerCitasDelDia` (calendar timeline), `CitaGoogleCalendarTab` (lazy fetch by `cita_id` from the new "Reunión" tab in `CitaDetalleModal`), `trigger-automatizaciones` (exposes it as the `cita.link_meet` variable for WhatsApp templates — empty string falls back to the automation's `default_value` or `'-'`). **Reschedule semantics**: on `update` op, the same Meet room is preserved (Google keeps `entryPoints` across event updates); `google_meet_url` is re-written defensively in case the event was recreated (when an event was deleted on Google but VentaPlay still has the map row, the sync recreates it). **Cancel semantics**: the Google event is deleted but the `google_meet_url` value stays on the cita row (audit trail). Bumps the `citas` reference in [appointments-calendar.md](references/appointments-calendar.md).

- **`organizaciones.cliente_email_obligatorio`** (same migration) — `boolean NOT NULL DEFAULT false`. Per-org toggle that makes the email field required when creating a client from the internal UI (`ClienteCreationModal` + `creation/BasicInfoStep.tsx`). The motivation is the Google Calendar attendee invite: to add the client as a Google Calendar attendee (so they get the Google reminder email + the Meet link in their personal calendar), VentaPlay needs their email. Off by default because most VentaPlay orgs are phone-first (WhatsApp confirmations are the norm). Configurable from the new `ClientesConfigDialog` accessible via the "Configuración" button in the Clientes section. **Validation lives on the frontend only** — the column doesn't add a DB-level NOT NULL on `contactos.email` (existing rows without email keep working; the toggle only gates new creations from the internal UI). Bumps the `organizaciones` reference in [organizations-config.md](references/organizations-config.md).

- **`organizaciones.crear_google_meet_citas`** (migration `20260612120000_crear_google_meet_citas.sql`) — `boolean NOT NULL DEFAULT false`. Per-org toggle that enables automatic Google Meet room creation for every cita synced to Google Calendar. Read by `google-calendar-sync/lib/eventBuilder.ts` when building the event payload — if true, the function adds `conferenceData.createRequest` with a random `requestId` and `conferenceSolutionKey.type='hangoutsMeet'`, and the POST is made with `conferenceDataVersion=1` (without this flag Google silently ignores the conference field). Opt-in (default false) because the typical VentaPlay org is a brick-and-mortar service business (peluquerías, spas, clínicas presenciales, restaurantes) — videollamada is the exception, not the norm. Admin-only toggle, configured from a new `GoogleCalendarConfigDialog` accessible via the "Google Calendar" Configuración button in the Profesionales section. **Scope of effect**: only orgs with this flag on get the conference field added to the Google event; orgs without it sync the cita with no Meet (legacy behavior). **Does NOT delete existing Meet rooms** when toggled off — only suppresses creation on future syncs. Bumps the `organizaciones` reference in [organizations-config.md](references/organizations-config.md).

**Frontend integration not in schema**: `useCitas.crearCita` was reordered so the Google Calendar sync runs BEFORE the WhatsApp automation dispatch (with a 5s timeout race). This ensures `citas.google_meet_url` is populated before `trigger-automatizaciones` reads it for the `cita.link_meet` variable. The same reorder was applied in `supabase/functions/manage-appointments/operations/create.ts` for the MCP/WhatsApp-bot path (single-shot `fetch` with `AbortSignal.timeout(5000)`). If the sync exceeds 5s, the cita is already created and the sync continues server-side — the WhatsApp template just gets the `default_value` for `{cita.link_meet}` instead of the real URL on that first send.

### Recent changes (2026-05-22 → 2026-05-24 — payment-state hardening + feriado exclusions + sesiones diagnostic)

Six migrations applied in the window between the ventana_minutos rollout and the Google Meet integration. Two are **critical production bug fixes** (real duplicate-charge incidents in customer orgs), one adds a schema column, and three are RPC consolidations/refactors. None destructive.

- **`ingreso_cuenta_como_pago(uuid)` predicate + cascading rewrites** (migration `20260607120000_fix_calcular_estado_pago_reverso_pos_failed.sql`) — **CRITICAL fix for duplicate-charge bug**. The payment-completeness formula was duplicated across 4 implementations (1 SQL + 3 TS), and all of them subtracted EVERY reverso unconditionally without checking the original ingreso's state. When a POS ingreso in `pos_status IN ('pending','failed','cancelled','timeout')` was anulado, the reverso still subtracted from the atención's `estado_pago` — but the original never contributed positively (the function already excluded it) → **double penalty** → atención stuck at `por_pagar` → cashier saw "por pagar" and charged again → real duplicate charge. **Customer impact**: Club La Barba, atención `a7b165bb` (Mane Argomedo), 13-may-2026. **Fix**: new canonical predicate `ingreso_cuenta_como_pago(uuid)` defines what counts as a positive payment; reversos only subtract if the original ingreso counts. Conservation guaranteed: `original=+X ⇔ reverso=-X`. Rewrites `calcular_total_pagado_atencion` (the numeric core) and `calcular_estado_pago_atencion` (the state-resolver, used by 3 triggers) on top of the predicate. **Two new RPCs**: `obtener_resumen_pago_atencion(uuid)` returns the full breakdown (total_ingresos, total_reversos, total_neto, total_giftcards, total_canjes, cantidad_ingresos, cantidad_reversos, ultimo_pago, tiene_reversos, saldo_pendiente, estado_pago) for the frontend — replaces the TS-side calculation in `useIngresosPorAtencion` + `packPaymentHelpers`. `recalcular_estado_pago_atencion(uuid)` calculates AND persists (idempotent — only UPDATEs when state changes) — replaces ~130 lines of TS in `IngresosService.actualizarEstadoPagoAtencion`. Functions still bear `SECURITY DEFINER` and grants stay broad (anon/authenticated/service_role) — consistent with dev posture. See [atenciones-sessions.md](references/atenciones-sessions.md) + [payments-billing.md](references/payments-billing.md).

- **`actualizar_estado_pago_trigger()` rewrite** (migration `20260609120000_fix_trigger_estado_pago_pos_status_race.sql`) — **CRITICAL fix #2 — also a real duplicate-charge incident**. The trigger `trigger_actualizar_estado_pago_ingresos` on `ingresos` had an early-return check that only watched `monto`, `tipo`, `motivo_anulacion`. When the async POS cron (`cron-check-pending-payments` / `check-pos-payment`) flipped `pos_status='pending' → 'completed'` minutes after the INSERT, the trigger hit the early-return → `atenciones.estado_pago` stayed stale at `por_pagar` → operator saw "por pagar" on the calendar and charged again → **real duplicate charge**. **Customer impact**: Tonos, 15-may-2026, Lorena Sierra, $76.980 over-charged. **Additionally**, the function didn't handle multi-payment: when `ingresos.atencion_id IS NULL` AND `es_pago_multiple = true` (a payment that distributes across N atenciones via the `ingreso_atenciones` junction), it couldn't find an atención and recalculated nothing. **Fix**: added `pos_status` to the early-return predicate (trigger now SI reacts to `pending → completed` transitions) + added a multi-payment handler that iterates `ingreso_atenciones` and recalculates each atención on the bridge, with per-iteration BEGIN/EXCEPTION so one failure doesn't abort the others. Trigger object unchanged — only function body rewritten via `CREATE OR REPLACE`. See [payments-billing.md](references/payments-billing.md).

- **`get_ingresos_con_datos_v2` patch for `propina_directa`** (migration `20260608120000_get_ingresos_con_datos_v2_propina_directa_fecha_pago.sql`) — minimal patch on top of the v2 from `20260417150000`. Cierre Diario calls this RPC with `por_fecha_atencion=true`; in that mode the date filter uses `a.fecha_inicio` (the atención). But a `propina_directa` has no atención (`atencion_id IS NULL`) → `a.fecha_inicio IS NULL` → `NULL >= fecha_desde` is never TRUE → the propina_directa row was silently EXCLUDED from Cierre Diario → tip never counted in the cash drawer. **Fix**: added a new branch to the WHERE CASE — `WHEN i.origen_tipo = 'propina_directa' THEN i.fecha_pago` — placed BEFORE the `por_fecha_atencion` branch so first-match-wins. Mirrors the `venta_externa` branch (both are ingresos without atención). Signature/RETURNS TABLE identical → `CREATE OR REPLACE` preserves GRANTs. See [payments-billing.md](references/payments-billing.md).

- **`organizacion_bloqueos.profesionales_excluidos`** (migration `20260610120000_organizacion_bloqueos_profesionales_excluidos.sql`) — `uuid[] NOT NULL DEFAULT '{}'::uuid[]`. Allows specific professionals to be EXCLUDED from an org-wide feriado: an excluded profesional keeps a fully available agenda during the bloqueo's range; every other profesional stays blocked exactly as before. Default `'{}'` (empty) → feriado applies to ALL profesionales (preserves prior behavior for every existing row — zero behavior change on backfill). **Server-side enforced** via rewrites of `obtener_slots_con_incremento_v2` (bloqueos_organizacion CTE now skips rows where `profesionales_excluidos @> ARRAY[p_profesional_id]`) AND `validar_capacidad_cita` (same exclusion predicate added to the org-bloqueo lookup). UI bypass impossible — exclusion is honored at the DB level. Function bodies are byte-identical to the prior latest version `20260604120000_add_ventana_minutos_to_servicios.sql` except for the single WHERE predicate addition (the ventana_minutos logic is preserved verbatim). Signatures unchanged → no DROP needed. See [appointments-calendar.md](references/appointments-calendar.md).

- **`obtener_proxima_hora_por_servicio_batch` honors feriados** (migration `20260610130000_proxima_hora_batch_feriados.sql`) — follow-up that fixes a long-standing inconsistency: since the single-pass rewrite in `20260424213212_cupos_batch_single_pass.sql` (which predates `organizacion_bloqueos` from `20260427120000`), this batch RPC has NEVER checked feriados — only `bloqueos_recurrentes`. So the "próxima hora" chip in `SelectorProfesionalModal` → `useProximaHoraServicioBatch` could suggest a slot falling on a feriado. **Not a double-booking risk** (the actual booking still got rejected by `obtener_slots_con_incremento_v2` + `validar_capacidad_cita`) — just a UI-consistency bug that caused user confusion. Mirrors `obtener_slots_con_incremento_v2`'s feriado handling: `profesional_config` CTE now also selects `organizacion_id`; new `bloqueos_organizacion` CTE (date-range overlap, America/Santiago tz) with the same `profesionales_excluidos` exclusion predicate; `slot_decision` marks a slot unavailable when it overlaps a non-excluded feriado. Depends on `20260610120000`. Body byte-identical to `20260605120000_proxima_hora_batch_ventana.sql` except for the additions. Signature unchanged. See [appointments-calendar.md](references/appointments-calendar.md).

- **`summary_sesiones_por_navegador()` RPC** (migration `20260606120000_summary_sesiones_por_navegador.sql`) — aggregates active `user_sessions` (where `revoked_at IS NULL AND expires_at > now()`) grouped by raw `user_agent` string. Returns `(user_agent, session_count, last_seen_at, organizacion_ids uuid[], sample_ips text[]` where IPs are deduplicated, host-formatted, and capped at 5 samples). Powers the new "Navegadores" tab in the super-admin `/sesiones` page; the frontend parses each `user_agent` through `parseUserAgent.ts` and merges rows that collapse to the same `(browser, OS)` tuple. **Aggregating by raw string (not by a parsed key)** is intentional — avoids porting `parseUserAgent` into PL/pgSQL and keeping two implementations in sync. **Auth posture**: SECURITY INVOKER — preserves the Tier 1c RLS posture on `user_sessions` (permissive `USING (true)`, with the super-admin gate enforced at the page level via `isSuperAdmin` in the React layer). No org/user filtering — Navegadores is a platform-wide diagnostic surface. Granted to anon, authenticated.

### Recent changes (2026-05-14 → 2026-05-19 — Google Calendar Phase 1 + restaurant comanda printing + sales fix)

Four migrations that landed between the Tier 1c session work and the ventana_minutos rollout. The Google Calendar integration is the headline — 3 new tables that increased the `public` schema count from 159 to 162.

- **Google Calendar Integration — Phase 1 (VentaPlay → Google, unidirectional)** (migration `20260526120002_google_calendar_integration.sql`) — **3 new tables, 1 VIEW, 3 RLS policies, 1 trigger function, 6 indexes**. Bumps the total table count to 162 and adds the new `google_calendar_*` tables to the Appointments & Calendar domain (now 9 tables). The tables:
  - **`google_calendar_connections`** — one row per profesional that has linked their Google account. Stores OAuth tokens in plaintext (Phase 1 posture — service-role-only via RLS, mirrors `configuracion_whatsapp_business.meta_token`). Includes Phase 2 fields (sync_token, watch_channel_id, etc.) pre-allocated as nullable to avoid future migrations. Per-connection settings: `default_reminders_minutes int4[] DEFAULT {60, 1440}` (1h + 1day), `event_color_id`, `include_client_email`. Status machine: `active | token_revocado | token_expirado | error | disconnected`.
  - **`google_calendar_event_map`** — cita ↔ Google event mapping with retry tracking. UNIQUE on `cita_id` (one Google event per cita) and on `(connection_id, google_event_id)`. CASCADE on `cita_id` means a deleted cita silently orphans Google events — by design, since cita deletion is rare and the edge function explicitly handles cancel via the `delete` op BEFORE deleting the local row.
  - **`google_calendar_sync_log`** — append-only audit log. `bigserial` PK, `direction` (`outbound` Phase 1, `inbound` Phase 2-ready), `operation` (`create | update | delete | refresh_token | watch_renew | full_sync | disconnect`), `result` (`success | error | skipped | retry_scheduled`), full `request_payload` / `response_payload` as jsonb, plus `duration_ms` for performance tracking. FKs use SET NULL so the audit survives connection/cita deletion.
  - **`google_calendar_connections_public` VIEW** — token-stripped projection for the frontend. **SECURITY INVOKER = FALSE** (intentional) — VIEW runs as owner (postgres) and bypasses RLS. Required because the project's custom-auth GUC resets between requests due to Supabase pooling; during post-reload rehydration there's a race where the query can fire before the GUC is set → `get_current_custom_user_organization()` returns the zero UUID → RLS blocks the read. Trade-off: authenticated users from any org can read connection metadata (no tokens, just `google_email` + status + settings) — acceptable because no tokens or client data leak.
  - **RLS**: all 3 tables have SELECT policies `USING (organizacion_id = get_current_custom_user_organization())` (event_map and sync_log filter via connection_id). NO INSERT/UPDATE/DELETE policies — all writes go through edge functions with service_role.
  - **Edge functions consuming these tables**: `google-oauth-init`, `google-oauth-callback`, `google-calendar-sync`, `google-calendar-disconnect`, `google-calendar-token-refresh`, `google-calendar-webhook` (Phase 2 stub).
  - See [appointments-calendar.md](references/appointments-calendar.md#google-calendar-integration-phase-1-ventaplay-→-google-unidirectional) for the full schema breakdown.

- **`atencion_platos.comanda_impresa` + `consumos.comanda_impresa`** (migration `20260603120000_comanda_impresa_flag.sql`) — `boolean NOT NULL DEFAULT false` added to both tables. Powers the restaurant kitchen-printer loop: the PC with the comanda printer polls for `comanda_impresa = false`, prints the ticket, and flippea to `true`. Non-printer machines never touch these columns. **Backfill at deploy**: items in finished/cancelled atenciones (`estado NOT IN ('en_curso', 'reabierto')`) OR items already past the kitchen state (`estado_pedido->>'estado' = 'LISTO'`) were pre-marked `true` to prevent a one-time reprint storm of historical orders when the printer client came online. See [atenciones-sessions.md](references/atenciones-sessions.md#atencion_platos) + [sales-products.md](references/sales-products.md#consumos).

- **`resumen_ventas_por_producto` excludes canceladas** (migration `20260526120000_fix_resumen_ventas_excluir_canceladas.sql`) — fix on top of the original `20260424120000`: the `atenciones_pagadas` CTE only filtered by `ingresos.created_at` without checking `atenciones.estado`. Atenciones cancelled AFTER payment (late-anulación cases) showed up in the sales summary even though they don't appear in the regular atenciones list. **Fix**: JOIN with `atenciones` + explicit `WHERE a.estado != 'cancelada'`, consistent with the default filter of the atenciones list. Signature/RETURNS TABLE unchanged → `CREATE OR REPLACE` preserves GRANTs.

- **One-shot data cleanup** (migration `20260602120000_clear_orphan_precuenta_pending.sql`) — single idempotent `UPDATE atenciones SET precuenta_print_pending = false WHERE precuenta_print_pending = true AND estado = 'cancelada'`. The auto-printing drainer ignores cancelled atenciones (excludes `estado = 'cancelada'`), but the flag was getting orphaned indefinitely on cancelled rows due to a race in the cancellation flow. Pure cleanup — no schema change, no behavior change going forward.

### Recent changes (2026-05-13 — servicios.ventana_minutos rollout)

Per-servicio post-cita buffer that blocks the profesional's agenda for N minutes after a cita that includes the servicio. Default 0 → existing behavior byte-equivalent.

- **`servicios.ventana_minutos`** (migration `20260604120000_add_ventana_minutos_to_servicios.sql`) — `integer NOT NULL DEFAULT 0` with `CHECK (ventana_minutos >= 0)`. Per-servicio buffer in minutes. **Universal block (Layer 1)**: when a cita ends, the profesional cannot accept ANY cita (of any servicio) during `[hora_fin, hora_fin + ventana_minutos)`. **`cita.hora_fin` is NEVER modified** — ventana is a phantom occupation visible only to the slot RPC and capacity trigger. Calendar render, atencion duration, WhatsApp/email confirmations, billing, and reports keep reading the real `hora_fin`. **Pack exception**: inside the same `pack_grupo_id`, ventana on a NON-last item is suppressed (sibling EXISTS guard with `hora_inicio >= cr.hora_fin`); fires only after the LAST pack item. **Pack-level ventana = silent no-op**: `cita.servicios[].id` stores SUB-servicio UUIDs (never the parent pack's), so the lookup never finds a pack's value. The frontend (`ServicioInformacionInlineEdit`) hides the ventana field when `tipo='pack'` and force-writes 0 on save. **Bumps `servicios` reference in [services-professionals.md](references/services-professionals.md).** No new tables; total stays at 159.

- **`obtener_slots_con_incremento_v2` rewrite** (same migration) — adds `citas_rango_extendidas` CTE that extends each cita's effective end to `hora_fin + max(servicios.ventana_minutos)` (looked up via `jsonb_array_elements(cr.servicios)`), with pack-non-last suppression. `ocupaciones_rango` now reads from the extended view; Layer 2 (`constraints_por_slot`) unchanged (ventana is Layer 1 only). Midnight wrap clamped via `least(..., time '23:59:59')`. Signature unchanged → CREATE OR REPLACE.

- **`validar_capacidad_cita` trigger rewrite** (same migration) — the Layer 1 occupancy count subquery now matches each prior cita when `c.hora_fin > minuto` (real overlap) OR `minuto < c.hora_fin + ventana_minutos` (ventana tail), with the same pack-non-last EXISTS suppression. NEW cita's own ventana doesn't apply to itself (only to future bookings). Trigger fires on `BEFORE INSERT OR UPDATE OF profesional_id, fecha_cita, hora_inicio, hora_fin, estado` — covers every cita write path including pack RPC and service-role edge functions.

- **`obtener_proxima_hora_por_servicio_batch` follow-up** (migration `20260605120000_proxima_hora_batch_ventana.sql`) — the próxima-hora hint RPC carries its own duplicated `citas_rango` CTE (doesn't delegate to v2). Round 1 missed it, so the Selector Profesional modal showed pre-ventana próxima hora while ServicioCard correctly respected it. Rewrites the batch RPC with the same `citas_rango_extendidas` pattern + pack-non-last guard. Layer 2 `cita_side_constraints` continues to join `citas_rango` (ventana stays Layer 1). Signature unchanged.

- **Critical FK / data invariant**: `citas.pack_grupo_id` is the discriminator for pack siblings. Ventana suppression relies on same-day same-`pack_grupo_id` siblings with `hora_inicio >= this.hora_fin`. If pack items are ever inserted out of chronological order, item N's ventana could fire against item N-1 — `crear_citas_pack` ([20260408172518](../../../../supabase/migrations/20260408172518_crear_citas_pack_rpc.sql)) iterates `jsonb_array_elements` in array order, and the frontend caller orders items by `hora_inicio` ASC, so this is consistent in practice. Hardening (`ORDER BY hora_inicio` inside the RPC) is tracked as a follow-up.

- **Pre-ventana citas**: existing rows with `tipo='pack' AND ventana_minutos > 0` (if any survived the form-side reset) are no-ops because the lookup never matches the pack UUID. Optional one-time cleanup: `update public.servicios set ventana_minutos = 0 where tipo = 'pack' and ventana_minutos > 0;` (not required, purely cosmetic).

### Recent changes (2026-05-12, late II — post-Tier-1c-push)

After the original Tier 1c rollout, 5 follow-up migrations shipped same-day to support the super-admin Sesiones page + ops fixes; additionally 9 previously-remote-only migrations were recovered into local files during a sync-cleanup pass. All 20 are now applied on both local + remote.

**Tier 1c follow-ups:**

- **`revoke_session_by_id(uuid, text)` RPC** (migration `20260528120000`) — per-session revoke for the super-admin Sesiones page; idempotent (`RETURNS false` if already revoked); auth gate is `super_admin OR same-org`. Granted to anon, authenticated.

- **`user_sessions.organizacion_id` FK + index** (migration `20260529120000`) — adds `FOREIGN KEY (organizacion_id) REFERENCES organizaciones(id) ON DELETE SET NULL` + partial index `idx_user_sessions_organizacion_active`. **Required for PostgREST embed** `organizaciones:organizacion_id (...)` used by the super-admin Sesiones page; without the FK, queries error with "Could not find a relationship". Snapshot-at-login semantics preserved (column only nullifies on hard org-delete).

- **`user_sessions` realtime publication** (migration `20260530120000`) — added to `supabase_realtime` publication + `REPLICA IDENTITY FULL`. Required for the AuthContext per-session-revoke kick channel (`user-sessions-${userId}` postgres_changes UPDATE filtered by `user_id`).

- **`user_sessions` RLS flipped to permissive** (migration `20260531120000`) — DROPped restrictive `user_sessions_select_own` policy, replaced with `user_sessions_select_all USING (true)`. **Critical reason**: Supabase Realtime evaluates SELECT policies as the **anon** role (Tier 1c keeps WebSocket auth on anon — opaque tokens can't be passed via WebSocket headers). Restrictive policy filtered out every broadcast → per-session revoke kick was inconsistent. With permissive RLS, identity gate moves entirely to the X-Session-Token / RPC layer (matches dev-mode posture of usuarios + other tables). When future Tier 2 (JWT) lands, this can be tightened back AND `realtime.setAuth(jwt)` called together.

- **`login_user` IP + user_agent header derivation** (migration `20260601120000`) — `CREATE OR REPLACE FUNCTION` keeps signature unchanged but body now derives `ip` + `user_agent` from `request.headers->>'x-forwarded-for'` and `request.headers->>'user-agent'` when caller doesn't pass them explicitly. Closes the "all session rows have NULL ip + user_agent" bug — frontend `AuthContext.login` only passes `(email, password)`, the RPC fills the rest from the proxy headers PostgREST already collects per-request. Local supabase-cli may show Docker proxy IP (e.g. `172.21.0.1`); production sets real client IP via Supabase edge proxy XFF.

**Recovered migrations (originally applied directly via Studio on remote May 11–13, then re-saved as local files during sync cleanup):**

- **`20260511174648_add_display_name_to_roles`** — `roles.display_name varchar(100) NULL`. UI can show custom Spanish-language label per role; falls back to canonical name when NULL.
- **`20260511175036_fix_display_name_backfill_roles`** — data cleanup `UPDATE roles SET display_name = NULL WHERE display_name = name`. Reverts an earlier copy-name-into-display_name backfill so the resolver falls back to the canonical Spanish label (e.g. "Recepcionista" instead of "receptionist").
- **`20260511184628_add_restaurantes_permissions`** — 8 new rows in `permissions` catalog: `restaurantes.view`, `restaurantes.atender`, `restaurantes.cobrar`, `restaurantes.cancelar`, `restaurantes.cocina`, `restaurantes.menu`, `restaurantes.platos`, `restaurantes.admin`. Wired through PermissionsService for granular role-based control of the Restaurant module.
- **`20260512180534_add_precuenta_print_pending`** — `atenciones.precuenta_print_pending boolean NOT NULL DEFAULT false`. Restaurant flow: marks an atencion as needing the pre-cuenta receipt printed by the next available terminal.
- **`20260512202140_add_imprimir_en_comanda`** — `categorias_productos.imprimir_en_comanda boolean NOT NULL DEFAULT true`. Per-category opt-out for kitchen comanda printing (e.g. drinks shouldn't print to cocina).
- **`20260512222458_add_mermas_pedido` + `20260512222606_fix_mermas_pedido_rls`** — NEW TABLE `mermas_pedido` (immutable audit log of pedido item deletions with motivo: `Error|Merma|Reclamo|Cambio|Otro`). Columns: id, organizacion_id, atencion_id, mesa_nombre, tipo_item CHECK(`plato`|`consumo`), item_id, item_nombre, cantidad, precio_unitario, subtotal, motivo, notas, cancelado_por, cancelado_por_nombre, created_at. Indexes: `(organizacion_id, created_at DESC)`, `(atencion_id)`. RLS: enabled but permissive `USING (true)` (the fix migration relaxed the original org-scoped policy). **No explicit FK constraints** on org/atencion/cancelado_por — they're plain UUIDs (matches the codebase's restaurant-domain convention of denormalizing for audit-log immutability). Bumps Atenciones (Sessions) domain to 9 tables.
- **`20260512224140_add_finalizado_por_to_atenciones`** — `atenciones.finalizado_por_id uuid NULL`, `atenciones.finalizado_por_nombre text NULL`. Records who closed/finalized the atencion (denormalized name for audit-log resilience to user deletion).
- **`20260513040225_enable_realtime_atenciones`** — `ALTER PUBLICATION supabase_realtime ADD TABLE atenciones`. Restaurant cocina/mesa live updates depend on this.

**Other recent additions:**

- **`20260524120000_add_chat_view_permission`** — single row in `permissions` catalog: `chat.view`. Gates the internal-conversations area in the sidebar/permission system.

Bumps Atenciones (Sessions) domain to 9 tables. Public schema goes 158 → 159 tables, 284 → 285 FK relationships (only `user_sessions.organizacion_id_fkey` added).

### Recent changes (2026-05-12)

- **Tier 1c — custom session management** (migrations `20260525120000`, `20260525120100`, `20260526120000`, `20260526120100`, `20260527120000`) — replaces the spoofable `set_current_user_id` GUC pattern with server-verified opaque session tokens carried in an `X-Session-Token` HTTP header.

  **New table `user_sessions`** (Auth & Roles): id, user_id (FK → usuarios.id ON DELETE CASCADE), organizacion_id (snapshot at login, NULL for super_admin), token_hash UNIQUE (SHA256 hex), created_at, last_seen_at, expires_at, revoked_at, revoked_reason CHECK(`logout`|`admin_disable`|`password_reset`|`admin_revoke`|`expired`), ip inet, user_agent text. CHECK `expires_at > created_at`. Partial indexes on `(token_hash) WHERE revoked_at IS NULL`, `(user_id) WHERE revoked_at IS NULL`, `(expires_at) WHERE revoked_at IS NULL`. RLS enabled with policy `user_sessions_select_own` (`user_id = get_current_custom_user_id() OR is_super_admin()`). No UNIQUE on user_id → multi-device by design. 24h sliding window via `bump_session_last_seen()`.

  **New RPCs** (all SECURITY DEFINER, granted to `anon, authenticated`):
  - `login_user(user_email text, user_password text, ip_address inet DEFAULT NULL, client_user_agent text DEFAULT NULL)` — extended signature (4 args). Returns `(id, nombre, email, rol, organizacion_id, activo, deshabilitado, landing_subdomain, session_token, session_id, expires_at)`. Issues a 256-bit opaque token via `extensions.gen_random_bytes(32)` (base64), inserts row in `user_sessions`, returns plaintext token to client (only place it exists in plaintext). When user is `deshabilitado=true` OR `activo=false`, returns user record with NULL token (login blocked).
  - `validate_session() → (valid boolean, reason text, user_id uuid)` — reads token from `request.headers`, looks up session, returns reason in `{ok, no_token, session_not_found, revoked, expired, user_not_found, disabled, password_reset_pending}`. Used by frontend `enforceSessionStatus`.
  - `revoke_current_session() → void` — logout; marks caller's session row `revoked_at=now(), revoked_reason='logout'`.
  - `revoke_all_sessions_for_user(p_user_id uuid, p_reason text DEFAULT 'admin_revoke') → int` — admin-only (super_admin OR same-org). Mass revoke. Returns row count. Wired to admin-disable handler so disabled users lose every device.
  - `bump_session_last_seen() → void` — slides `expires_at = greatest(expires_at, now() + 24h)`. Frontend calls debounced ≤1×/min.
  - `_current_session_token() → text` STABLE — internal helper; reads `request.headers::jsonb ->> 'x-session-token'`. Returns NULL if missing.
  - `hash_session_token(p_token text) → text` IMMUTABLE — SHA256 hex of an opaque token.

  **`get_current_custom_user_id()` body REWRITTEN** — phase 1 (transition) read header-first with GUC fallback; phase 2 (finalize, `20260526120001`) dropped the GUC fallback entirely. Now ONLY reads from the X-Session-Token header → looks up `user_sessions.token_hash` → returns `user_id` if (not revoked AND not expired) else sentinel `00000000-0000-0000-0000-000000000000`. **`get_current_custom_user_organization()` and `is_super_admin()` keep working unchanged** — they SELECT through `get_current_custom_user_id()`, so all ~50 RLS policies that use the org helper transparently inherit the new identity source. Zero policy rewrites needed.

  **`set_current_user_id(uuid) DROPPED`** — phase 2 finalize migration removed the function entirely. Devtools spoof attack `await supabase.rpc('set_current_user_id', { user_id: '<admin>' })` now throws `function does not exist`. Any SQL function that internally called it (only `login_public_user` at `20260411210000:86-91`) now hits its EXCEPTION handler and logs `RAISE NOTICE` but continues normally — cosmetic log noise, no functional impact.

  **`upsert_cotizacion_con_items` rewritten** (migration `20260525120100`) — derives `usuario_id` from `get_current_custom_user_id()` (header). Ignores `p_cotizacion->>'usuario_id'` from the body. Throws `'authentication required'` if no valid session. Closes the prior body-trust vulnerability where any caller could write cotizaciones as any user. Frontend can keep sending `usuario_id` in payload — RPC ignores it for backwards compat.

  **`usuarios` realtime publication** (migration `20260527120000`) — `usuarios` added to `supabase_realtime` publication with `REPLICA IDENTITY FULL`. Codifies the dashboard-added publication membership so `supabase db reset` reproduces it. Required for the AuthContext disable-detection channel `usuario-status-${userId}` postgres_changes UPDATE subscription to deliver `payload.new.deshabilitado` / `payload.new.activo` (default REPLICA IDENTITY only ships PK).

  **pg_cron `sweep-user-sessions`** (migration `20260526120100`) — hourly DELETE of revoked OR expired sessions older than 7 days (forensics retention window).

  **Rollback** at repo root: `rollback_tier1c_sessions.sql`. Manual `psql -f` restores `set_current_user_id`, restores old `login_user` 8-col signature, restores body-trust `upsert_cotizacion_con_items`, restores GUC-only `get_current_custom_user_id`, drops `user_sessions` and all 6 new RPCs. Idempotent.

  Bumps Auth & Roles domain to 7 tables. See [auth-roles.md](references/auth-roles.md). The `upsert_cotizacion_con_items` cotizaciones RPC — and the cotizaciones vertical in general — is documented in the **`ventaplay-taller`** skill (not in `payments-billing.md`).

- **`user_sessions.organizacion_id` FK** (migration `20260529120000_user_sessions_org_fk.sql`, same-day fix) — adds `FOREIGN KEY (organizacion_id) REFERENCES organizaciones(id) ON DELETE SET NULL` + partial index `idx_user_sessions_organizacion_active`. **Required for PostgREST embed** `organizaciones:organizacion_id (...)` used by the super-admin Sesiones page; without the FK, queries error with *"Could not find a relationship between 'user_sessions' and 'organizacion_id' in the schema cache"*. Snapshot-at-login semantic preserved — column only nullifies if org is hard-deleted (rare); user_id audit trail stays intact via `ON DELETE CASCADE` on `user_id`. Migration includes a defensive `UPDATE … SET organizacion_id = NULL WHERE organizacion_id NOT IN (SELECT id FROM organizaciones)` to clean any orphans before applying the FK (no-op in practice).

### Recent changes (2026-05-08, late)

- **`get_atenciones_rows_batch` v4 — adds `packInfo` bucket** (migration `20260521120000_get_atenciones_rows_batch_v4_packinfo.sql`) — `CREATE OR REPLACE FUNCTION` that keeps SECTION 1-6 (FRESCA / BOLETAS / GIFTCARDS / CANJES / PAGOS / PROPINAS) byte-equivalent to v3 and adds **SECTION 7 PACKINFO**: `LEFT JOIN citas ON c.id = a.cita_id` + `LEFT JOIN servicios ON s.id = c.pack_servicio_id` to resolve `{ esPack, packComisionIndividual }` per atencion. Only includes atenciones that ARE pack in the bucket; non-pack atenciones get `{ esPack: false, packComisionIndividual: false }` defaults from the client `seedCache`. **Eliminates the N+1 cascade in `/atenciones`**: previously `useAtencionCardData` fired one `citas?select=pack_grupo_id,pack_servicio_id&id=eq.<UUID>` request per AtencionCard plus one `servicios?select=pack_config&id=eq.<UUID>` per pack card (~50 requests for a 30-row list, with the `servicios` ones all duplicated to the same UUID since React Query keys by `cita_id`, not by `pack_servicio_id`). v4 absorbs all of that into the existing batch RPC + `seedCache` writes `['cita-pack-info', cita_id]`. **Backwards-compat both directions**: old frontend reading missing `data.packInfo` → fallback to direct fetches (current behavior); new frontend hitting old RPC → same fallback. Same signature `(uuid, uuid[]) RETURNS jsonb` and same attributes `STABLE SECURITY INVOKER` → grants/permisos preservados. Edge cases: `cita_id IS NULL` → atencion omitida del bucket (hook hace early return); `pack_grupo_id IS NOT NULL pero pack_servicio_id IS NULL` (legacy) → `esPack=true, packComisionIndividual=false`; servicio padre eliminado / `pack_config` malformado → `packComisionIndividual=false`. See [atenciones-sessions.md](references/atenciones-sessions.md).

### Recent changes (2026-05-09)

- **`servicio_categorias` table + `servicios.categoria_id` FK** (migration `20260518120000_add_servicio_categorias.sql`) — new single-categoría grouping layer above the many-to-many `servicio_tags`. Schema: `id`, `organizacion_id` (CASCADE), `nombre varchar(100)`, `orden int4 NULL`, timestamps; `UNIQUE(organizacion_id, nombre)`. `servicios` gains `categoria_id uuid NULL REFERENCES servicio_categorias(id) ON DELETE SET NULL`. Indexes: `idx_servicio_categorias_org`, partial `idx_servicio_categorias_orden_org`, partial `idx_servicios_categoria_id`. RLS = permissive `USING (true) WITH CHECK (true)` mirroring `servicios` + `servicio_tags`. **Public-landing only consumer in v1**, gated by `organizaciones.landing_config.sections.services.config.useCategorias`. When ON, `StepServicios` renders a Radix `Accordion type="single" collapsible` (all closed by default, one-at-a-time) bucketed by `categoria_id`; orphan FKs and NULLs route to a synthetic "Sin categoría" panel rendered last. Canonical list comes from a dedicated `landing-servicio-categorias` fetch in `useLandingData` (NOT the PostgREST embed, which can null silently). **Critical gotcha**: profesionales-embedded `servicios(...)` projection in `useLandingData.ts` and `useLandingDataWithTags.ts` MUST explicitly enumerate `categoria_id` — `AccordionBookingContent.serviciosPublicosFiltrados` builds StepServicios input from `profesional.servicios`, not from the org-wide `useLandingData.servicios` query. Bumps Services & Professionals domain to 19 tables. See [services-professionals.md](references/services-professionals.md).

### Recent changes (2026-05-08)

- **`campanas_criterios` table + `replace_campana_criterios` RPC** (migration `20260520120000_campanas_criterios_v3.sql`) — **Targeting v3** for the Campañas module. Replaces the old fixed-3-filters model (`campanas_targets` for tags + estados, `campanas.fecha_atencion_*` for date range, all combined with forced OR) with a **dynamic list of criteria connected by user-modifiable `Y`/`O` operators evaluated left-to-right**. Schema: `id`, `id_campana` (CASCADE), `tipo` CHECK(`enviar`|`sacar`), `posicion int>=0`, `categoria` CHECK(`tag`|`funnel`|`fecha_atencion`), `valor_id uuid NULL` (for tag/funnel), `valor_jsonb NULL` (for fecha_atencion: `{desde,hasta,modo}` with **YYYY-MM-DD strings**, NOT timestamptz), `operador_siguiente` CHECK(`Y`|`O`) NULL only on last, timestamps. CHECK `valor_check` enforces `(categoria IN ('tag','funnel') AND valor_id NOT NULL AND valor_jsonb NULL) OR (categoria='fecha_atencion' AND valor_id NULL AND valor_jsonb NOT NULL)`. UNIQUE `(id_campana, tipo, posicion)`. **Two sections per campaña**: `enviar` (include — must have ≥1 criterio) and `sacar` (exclude — optional). Final audience = `enviar set MINUS sacar set`. RPC `replace_campana_criterios(p_id_campana bigint, p_organizacion uuid, p_criterios jsonb) → void` does atomic delete+insert in plpgsql transaction (closes the silent-zero-criterios bug from the old delete-then-insert client pattern); validates `p_organizacion = campanas.organizacion_id` and raises `'campana_no_pertenece_a_organizacion'` (42501) if mismatch. **No RLS** — coherent with parent `campanas` and `clientes`/`contactos`; cross-org defense lives in app filter + RPC validation + the `inner join cliente_tags` in `resolverCriterio`. Backfill in the migration converts every existing campaña to N criterios connected by `O` (preserves OR semantics) — idempotent via `WHERE NOT EXISTS`. **Critical gotcha**: the backfill MUST `::date` cast `campanas.fecha_atencion_*` (timestamptz) before serializing to jsonb, otherwise `<Input type="date">` in the builder silently fails to render the date. **Deprecates** `campanas_targets` and `campanas.fecha_atencion_desde/hasta/tipo_filtro_atencion` — both still populated but no code reads them post-v3. A future migration will drop them. Bumps Campaigns & Surveys domain to 10 tables. See [campaigns-surveys.md](references/campaigns-surveys.md).

### Recent changes (2026-05-04)

- **`idx_flujos_pasos_ejecucion_created_at`** (migration `20260504120000_flujos_pasos_ejecucion_created_at_index.sql`) — btree index on `flujos_pasos_ejecucion.created_at`, built `CONCURRENTLY` (migration uses the `-- supabase: disable-transaction` sentinel). Backs manual retention cleanup queries of the form `DELETE … WHERE created_at < now() - INTERVAL '1 month'`. The table had grown to ~8.77 GB without retention; this index makes the cleanup DELETE an ordered index scan instead of a full heap scan. **No automated cron** — pruning is paste-and-rerun in the Supabase SQL editor (see [flows-automations.md](references/flows-automations.md) notes). FK `fk_flujos_pasos_ejecucion_ejecucion` is `ON DELETE NO ACTION`, so deleting children does not cascade upward; pruning parent `flujos_ejecuciones` rows requires draining their children first. See [flows-automations.md](references/flows-automations.md).

### Recent changes (2026-04-30)

- **`organizaciones.usa_egreso`** (migration `20260430120000_add_usa_egreso_to_organizaciones.sql`) — `boolean NOT NULL DEFAULT false`. Per-org feature gate for the Egresos workflow on `/ingresos`. When true: "Crear Egreso" button renders, `useEgresosData` query fires, Tabla de Ingresos shows mixed `MovimientoCajaConDatos` (kind discriminator) sorted by `fecha_movimiento`, and Cierre Diario renders the "Total Egresos" KPI + subtracts from Caja Neta. When false: button hidden, query short-circuited via `enabled` opt, `egresosTotales` returns `undefined`, Excel/PDF exports skip the Egresos section, `data.movimientos` collapses to `data.ingresos`. Existing `egresos` rows are not deleted on toggle-off — only UI surfacing is gated. Wired through new `useOrgEgreso()` helper in `src/contexts/AuthContext.tsx`; toggle exposed in OrganizationForm. See [organizations-config.md](references/organizations-config.md).

### Recent changes (2026-04-29)

- **`egresos`** table + `egreso_medio_pago` / `egreso_tipo` enums + 2 RPCs (migration `20260429120000_add_egresos_table.sql`) — new domain entity for cash outflows that subtract from Cierre Diario neto. Decoupled from `ingresos` because `ingresos_origen_consistencia_check` requires `(atencion_id NOT NULL XOR venta_externa_id NOT NULL)` — egresos have neither, and the payment-side junctions (`ingreso_atenciones`, `propinas`, `giftcards`, `canjes`) carry semantics that don't fit expenses. Schema: `id`, `organizacion_id`, `usuario_id`, `sucursal_id` (nullable), `monto numeric (>0)`, `medio_pago egreso_medio_pago` (hardcoded enum: `efectivo` | `transferencia` | `comprobante_general` — NOT an FK to `medios_pago`), `notas text NOT NULL CHECK length>=1`, `fecha_egreso timestamptz`, `tipo egreso_tipo` default `'egreso'`, self-FK `egreso_original_id` for reverso flow, anulación columns (`motivo_anulacion`, `anulado_por_usuario_id`, `fecha_anulacion`). CHECK `egresos_reverso_consistencia` enforces `tipo='egreso'⇔egreso_original_id IS NULL`. Indexes: `(org, fecha_egreso DESC)`, `(org, tipo)`, `(org, sucursal_id)`, partial `(egreso_original_id) WHERE NOT NULL`. Dev RLS = wide-open (mirrors ingresos). RPCs: `get_egresos_con_datos_v1(org_id, fecha_desde, fecha_hasta, sucursal_id_filter)` returns enriched rows with `usuario_nombre`/`sucursal_nombre`/`anulado_por_nombre`/`tiene_reverso` (TZ-correct via `AT TIME ZONE 'America/Santiago'` before `::date`); `anular_egreso_con_reverso(p_egreso_id, p_motivo_anulacion, p_usuario_id)` is atomic plpgsql (locks original `FOR UPDATE`, INSERTs `tipo='reverso_egreso'` mirror, UPDATEs original anulación columns). Frontend integration: `EgresosService.calcularTotales` for Cierre KPIs; unified `MovimientoCajaConDatos` (kind discriminator) drives the mixed Tabla de Ingresos. Bumps Payments & Billing domain to 13 tables. See [payments-billing.md](references/payments-billing.md).

### Recent changes (2026-04-27 → 2026-04-28)

- **`servicios.orden` / `servicio_tags.orden`** (migration `20260428120000_add_orden_to_servicios_and_tags.sql`) — nullable `int4` columns adding optional public-landing display rank. Sorted `ASC NULLS LAST` only when the corresponding toggle in `organizaciones.landing_config.sections.services.config` is true (`orderServicios` for servicios, `orderTags` for tag chips in `StepServicios`). NULL rows go last. Reorder UI uses `OrdenColumnRenderer` (drag handle + up/down chevrons + clear) wired through the `useOrdenReorder` hook + `OrdenReorderProvider` context. Sequential 1..K renumber on every move; unranked stay NULL. Partial indexes `idx_servicios_orden_org` and `idx_servicio_tags_orden_org` on `(organizacion_id, orden) WHERE orden IS NOT NULL`. Internal CRM views ignore the field; only the public landing reads it. See [services-professionals.md](references/services-professionals.md).

### Recent changes (2026-04-24 → 2026-04-26)

- **`link_pagos` TUU + verification columns** (migrations `20260425123000`–`20260425220000`) — adds `tuu_tx_token uuid`, `tuu_instance uuid`, `notificado_pago_at timestamptz`, `pago_optimista_at timestamptz`, `tuu_account_id text`, `pago_verificacion_estado text` (CHECK NULL|`pending`|`verified`|`rejected`), `pago_verificacion_at timestamptz`, `pago_verificacion_intentos int` (default 0), `pago_verificacion_log jsonb` (default `[]`), `origen_url text`. New indexes `link_pagos_tuu_tx_token_idx`, `link_pagos_verificacion_pending_idx` (drives the async-verification worker queue), and `link_pagos_ref_unique` UNIQUE on `ref WHERE NOT NULL`. Implements TUU optimistic redirect + async verification (`verify-tuu-payment-background`). See [payments-billing.md](references/payments-billing.md).
- **`configuracion_link_pago.tuu_rut`** (migration `20260425140000_add_tuu_rut_and_link_pagos_ref_unique.sql`) — nullable `text` for the commerce RUT TUU/Webpay expects. Distinct from `organizaciones_facturacion`-level RUT. See [payments-billing.md](references/payments-billing.md).
- **`consumos.usar_precio_original_para_comision`** (migration `20260423220000_consumos_comision_valor_individual.sql`) — boolean NOT NULL DEFAULT false. When true, commission base flips from `precio_final` (prorated pack price) to `precio_original` (servicio individual snapshot). Activated only when a pack cita is created with `servicios.pack_config.comision_por_valor_individual = true` flowing through `create-pack-appointment` → AtencionesCRUD. **Asymmetry**: revenue totals still aggregate `precio_final` regardless. Partial index `idx_consumos_usar_precio_original`. See [sales-products.md](references/sales-products.md).
- **`resumen_ventas_por_producto` RPC** (migration `20260424120000_resumen_ventas_por_producto_rpc.sql`) — single-call sales summary combining `venta_productos_interna` + `ventas_externas_items` (estado `pagada`) + `consumos` of paid atenciones in a date range. Returns 10 columns ordered by `total_general DESC`. SECURITY INVOKER with custom-auth org guard (`get_current_custom_user_organization()` / `is_super_admin()`). Replaces ~5 round-trips + client-side aggregation. See [sales-products.md](references/sales-products.md).
- **`obtener_slots_con_incremento_v2` adds `p_only_first_available` + new `obtener_proxima_hora_por_servicio_batch` RPC** (migrations `20260424063319_cupos_earliest_mode.sql`, `20260424063320_proxima_hora_batch.sql`) — short-circuit param + a per-profesional batch RPC that loops over N servicios server-side, returning one earliest-slot row per servicio. Replaces `SelectorProfesionalModal`'s N×S client fanout that was hitting statement timeouts. Supporting indexes added in `20260424063318_cupos_perf_indexes.sql`: `idx_citas_servicios_gin`, `idx_citas_profesional_servicio_includes`, `idx_consumos_profesional_atencion`. See [appointments-calendar.md](references/appointments-calendar.md).
- **`validar_capacidad_cita` adds optional `p_servicios jsonb`** (migration `20260424002918_servicios_cupos.sql`) — when provided, the validator additionally enforces per-(profesional, servicio) cupo from `profesional_servicio.cupos`. Default NULL = backward compatible. The cupos column itself was first added on `servicios`, then moved to the `profesional_servicio` join table in `20260424002919_cupos_per_asignacion.sql` for per-assignment granularity. See [appointments-calendar.md](references/appointments-calendar.md) + [services-professionals.md](references/services-professionals.md).
- **`movimientos_stock.documento_tipo` / `documento_numero`** (migration `20260426130000_movimientos_stock_documento_compra.sql`) — nullable `varchar(30)` (CHECK NULL|`factura`|`guia_despacho`) + `varchar(80)`. Generalizes purchase-doc storage beyond invoices. Backfilled from `factura_numero` (sets `documento_tipo='factura'`, `documento_numero=btrim(factura_numero)`). Older un-normalized rows may still have only `factura_numero` set — read both. See [stock-inventory.md](references/stock-inventory.md).
- **`organizaciones.mostrar_propina_reporte_profesional`** (migration `20260424180000_add_mostrar_propina_reporte_profesional.sql`) — boolean NOT NULL DEFAULT false. Controls tip KPI + table column visibility on `/reporte-profesional`. Independent of `usa_propina` (collection vs. surfacing in pro report). See [organizations-config.md](references/organizations-config.md).
- **`ventas_externas.profesional_id`** (migration `20260423210000_ventas_externas_profesional.sql`) — nullable `uuid` FK to `profesionales.id` with partial index WHERE NOT NULL. Enables per-pro attribution of external sales. See [sales-products.md](references/sales-products.md).

### Recent changes (2026-04-21 → 2026-04-23)

- **`movimientos_stock.factura_numero` / `factura_monto`** (migration `20260423120000_add_factura_to_movimientos_stock.sql`) — nullable `varchar(80)` + `numeric(14,2)` for proveedor invoice data when `referencia_tipo='compra'`. New partial index `idx_movimientos_stock_ultima_compra` on `(producto_id, organizacion_id, created_at DESC) WHERE referencia_tipo = 'compra'` for fast "last purchase" lookups. See [stock-inventory.md](references/stock-inventory.md).
- **Derivaciones counted in slot + capacity validation** (migration `20260422120000_derivaciones_en_slots_y_capacidad.sql`) — new RPC `obtener_derivaciones_profesional_rango(p_profesional_id, p_fecha_inicio, p_fecha_fin)` returns `(cita_id, fecha_cita, hora_inicio, hora_fin)` for citas where another pro is primary but the atencion/servicio consumo is attributed to P. Redefines `obtener_slots_con_incremento_v2` and `validar_capacidad_cita` to UNION derivaciones with citas in occupancy math. **Asymmetry**: inserting a `consumos` row (the derivation side) is still unchecked — only cita create/update hits the validator. See [appointments-calendar.md](references/appointments-calendar.md).
- **`organizaciones_facturacion.tipo_sociedad` CHECK adds `LTDA`** (migration `20260421000000_add_ltda_to_tipo_sociedad_check.sql`) — constraint now allows NULL | `SPA` | `SA` | `Limitada` | `EIRL` | `LTDA`. Reason: users were typing `LTDA` as abbreviation for `Limitada`, causing 400 on save. See [organizations-config.md](references/organizations-config.md).
- **`servicios.comision_default`** (migration `20260421120000_add_comision_default_to_servicios.sql`) — nullable `jsonb` holding `{ tipo, valor, ultima_actualizacion }`. UI-only: pre-fills the form on the new servicio-side "Comisión" tab. Does NOT feed commission calculation — that still reads `profesionales.comisiones_json`. See [services-professionals.md](references/services-professionals.md).

### Recent changes (2026-04-15 → 2026-04-19)

- **`autorizaciones_otp`** table (migration `20260413200000_create_autorizaciones_otp.sql`) — new OTP authorization table for WhatsApp-verified sensitive actions. 6-digit bcrypt codes, 5-min expiry, `estado CHECK (pendiente|verificado|expirado|fallido)`, FK to `organizaciones` (CASCADE) + `usuarios` (SET NULL). Bumps Auth & Roles domain to 6 tables. See [auth-roles.md](references/auth-roles.md).
- **`automatizaciones_whatsapp.canal` / `email_template_id` / `variables_guardadas`** + **`automatizaciones_ejecuciones.canal`** (migration `20260415120000_add_email_channel_to_automatizaciones.sql`) — polymorphic channel discriminator `whatsapp`|`email`. `plantilla_nombre` dropped NOT NULL on both tables (email rows don't use it). `contacto_numero` on ejecuciones is polymorphic (phone vs email). See [whatsapp.md](references/whatsapp.md) + [flows-automations.md](references/flows-automations.md).
- **`atenciones.numero_comensales`** (migration `20260417160000_atenciones_numero_comensales.sql`) — nullable `smallint` with CHECK `1..999`, for restaurant covers count. See [atenciones-sessions.md](references/atenciones-sessions.md).
- **`plato_toppings.precio_carta`** (migration `20260417140000_plato_toppings_precio_carta.sql`) — nullable `numeric(12,2)` with CHECK `>= 0`. NULL falls back to legacy derivation (`precio_venta × cantidad_requerida` for "producto en carta" toppings, else `COALESCE(precio_extra, 0)`). See [atenciones-sessions.md](references/atenciones-sessions.md).
- **`get_ingresos_con_datos_v2` RPC** (migrations `20260416120000`, `20260417120000`, `20260417150000`) — adds `fecha_atencion timestamptz` and `profesionales_nombres text` return columns; uses `citas.sucursal_id → sucursales.nombre` as historic sucursal (falls back to professional's current sucursal); wraps `fecha_pago`/`fecha_inicio` in `AT TIME ZONE 'America/Santiago'` before `::date` for correct day-boundary filtering. Same TZ fix applied to `get_atencion_financial_stats` in `20260417130000`. See [payments-billing.md](references/payments-billing.md).
- **Bounce-handling RPCs + trigger on `clientes`** (migrations `20260418020000`, `20260418030000`, `20260419040000`, `20260419050000`) — `bulk_flag_bounced(email_list text[])` and `bulk_flag_email_verificado_pro(email_list text[])` for bulk email status updates (SECURITY DEFINER, no auth gate — UI-gated). Trigger `cliente_bounced_sync` cross-org-propagates `bounced=true` on matching `lower(btrim(email))`. Partial index `clientes_bounced_lower_email_idx` WHERE `bounced = true`. See [crm-contacts.md](references/crm-contacts.md).
- **`mark_bounced_envios_as_failed(p_execution_id bigint)` RPC** (migrations `20260419000000`–`20260419030000`) — flags pending `campana_envios` rows as `failed` when cliente bounced / email null-or-blank / email snapshot stale / orphan cliente_id. Note: final signature is `bigint` (the initial uuid variant was dropped). See [campaigns-surveys.md](references/campaigns-surveys.md).
- **pg_cron `flag-invalid-emails-as-bounced`** (migration `20260418010000_cron_flag_invalid_emails.sql`) — daily job (05:00 UTC) that auto-flags typosquatted/malformed emails on `clientes`. See [crm-contacts.md](references/crm-contacts.md).
- **`campana_envios` delivery aggregates** (migration `20260414130000_campana_delivery_aggregates.sql`) — new RPC `campana_delivery_counts(p_execution_id bigint, p_organizacion_id uuid, p_canal text) → jsonb` + composite indexes `idx_campana_envios_estado_status` and `idx_campana_envios_estado_created`. Replaces N client-side count queries for CampanaDeliveryDisplay. See [campaigns-surveys.md](references/campaigns-surveys.md).

### Recent changes (2026-04-13 → 2026-04-14)

- **`email_templates.template_type`** (migration `20260413120000_add_template_type_to_email_templates.sql`) — new discriminator column with CHECK (`builder` | `raw_html`) to support raw-HTML pasted templates alongside the block-based EmailBuilder flow.
- **`email_domains`** table (migration `20260413130000_add_email_domains_table.sql`) — new global registry of Resend sending domains with status, dns_records, Route53 tracking fields.
- **`configuracion_email.email_domain_id`** (migration `20260414100000_add_email_domain_to_configuracion_email.sql`) — nullable FK linking org sender configs to their assigned Resend domain.
- **`email_domains.dns_mode`** (migration `20260414110000_add_dns_mode_to_email_domains.sql`) — discriminates `route53` (auto-provisioned via `email-domain-manager` edge function in the `ventaplay.com` hosted zone) from `manual` (customer pastes records in their own DNS panel).
- **`campanas.canal` + `campanas.email_template_id`** (migration `20260414120000_add_canal_to_campanas.sql`) — channel discriminator (`whatsapp` default | `email`) + nullable FK to `email_templates`. Enables email campaigns sharing the existing `prepare-campana-send` + `send-campana-batch` pipeline. `campana_envios.contacto_numero` becomes polymorphic (phone for whatsapp, email for email) — documented via COMMENT, no schema split.
